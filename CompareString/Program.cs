using System;
using System.Text;

namespace CompareString
{
    class Program
    {
        // there are == and .Equals() which should I use?
        static void Main(string[] args)
        {
            // 我不知道有什么区别，我决定
            // 比较空时 用 == string.Empty
            // 比较相同时 用 .Equals()
            // https://docs.microsoft.com/zh-cn/dotnet/standard/base-types/comparing
        }
    }
}
